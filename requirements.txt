PyQt5>=5.15
opencv-python-headless>=4.5
numpy>=1.21
# pandas and xlsxwriter are only needed by the macOS script (VideoTimer.py)
pandas>=1.3
xlsxwriter>=3.0
# openpyxl is used by the Windows script (VideoTimerWindows.py) and as a macOS Excel fallback
openpyxl>=3.0
